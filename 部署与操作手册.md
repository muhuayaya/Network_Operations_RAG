# 部署与操作手册

本文用于在新电脑上部署、启动、验证和继续开发本项目。所有命令默认在项目根目录执行。

## 1. 项目定位

本项目面向企业 IT 与网络运维场景，提供一套带证据引用的知识检索与运维辅助系统。系统默认采用只读工作流，不提供设备配置写入或变更执行能力。

当前包含以下运行入口：

- CLI：环境检查、索引构建、数据摄取和运行时管理。
- FastAPI：健康检查、知识查询、配置差异比较等 REST 接口。
- Streamlit：知识检索与引用问答、事故场景回放、配置差异比较、追踪与评测页面。
- MCP stdio：通过受限的命名工具提供知识查询、配置差异和上下文读取能力。

## 2. 环境要求

### 必需软件

- Python 3.11，且版本低于 3.12。
- uv，用于创建环境和运行命令。
- Docker Desktop 或 Docker Engine，并启用 Docker Compose。
- Git（从远程仓库获取项目时需要）。

本文命令以 Windows PowerShell 为例。macOS/Linux 使用对应的 shell 语法替换 Set-Location、Invoke-RestMethod 和路径写法即可，项目本身的 Python 入口不变。

### 外部服务

完整的 local-milvus 运行路径需要以下服务：

| 服务 | 默认地址 | 用途 |
| --- | --- | --- |
| Milvus | 127.0.0.1:19530 | Dense 向量检索和 BM25 检索 |
| PostgreSQL | 127.0.0.1:5432 | 索引版本、活动索引和元数据 |
| MinIO | 由 Compose 内部使用 | Milvus 对象存储 |
| etcd | 由 Compose 内部使用 | Milvus 元数据服务 |

构建索引和生成引用问答需要配置 OpenAI-compatible 的 Embedding/LLM 服务。项目配置文件默认使用 DashScope 兼容接口；也可以按代码中的 provider 接口替换为其他兼容服务。

## 3. 获取项目并安装依赖

~~~powershell
git clone <仓库地址>
Set-Location Network_Operations_RAG

uv sync --all-groups
~~~

如果只使用 local-milvus，每次运行命令时加入 --extra local-milvus；如果使用 demo-lite，加入 --extra demo-index。也可以一次同步两组可选依赖：

~~~powershell
uv sync --all-groups --extra demo-index --extra local-milvus
~~~

确认 CLI 可用：

~~~powershell
uv run netops-copilot --version
uv run netops-copilot --help
~~~

## 4. 配置环境变量

在项目根目录新建 .env。该文件已被 Git 忽略，不应提交到仓库。下面是本地运行所需变量的结构，密码和 API Key 请替换为自己的值：

~~~dotenv
# Docker Compose
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=请设置一个长度足够的本地密码
POSTGRES_PASSWORD=请设置一个本地数据库密码

# local-milvus 连接
MILVUS_URI=http://127.0.0.1:19530
MILVUS_TOKEN=root:Milvus
POSTGRES_USER=netops
POSTGRES_DB=netops
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_CONNECT_TIMEOUT=5

# 默认的 Embedding/LLM 服务
DASHSCOPE_API_KEY=请填写自己的服务密钥

# 可选：使用本地 OpenAI-compatible 模型时填写
# LOCAL_LLM_BASE_URL=http://127.0.0.1:1234/v1
# LOCAL_LLM_MODEL=模型名称
# LOCAL_LLM_API_KEY=local
~~~

说明：

- docker-compose.yml 使用 MINIO_ROOT_USER、MINIO_ROOT_PASSWORD 和 POSTGRES_PASSWORD 启动依赖服务。
- POSTGRES_DSN 可以替代 POSTGRES_* 的连接参数；如果同时设置，程序优先使用 POSTGRES_DSN。
- DASHSCOPE_API_KEY 用于索引构建时的 Embedding，也用于默认的引用问答模型。
- 程序会读取当前工作目录下的 .env，环境变量本身的优先级更高。
- 不要把真实密钥写入源代码、Markdown、测试样例或提交记录。

## 5. 完整启动：local-milvus

这是项目的完整本地部署路径，使用 Docker 中的 Milvus 和 PostgreSQL。

### 5.1 启动依赖服务

确保 Docker Desktop 已启动，然后在项目根目录执行：

~~~powershell
docker compose up -d
docker compose ps
~~~

等待 etcd、minio、milvus、postgres 的状态稳定后再继续。需要查看启动原因时：

~~~powershell
docker compose logs --tail=80 milvus postgres
~~~

### 5.2 检查后端连接

~~~powershell
uv run --extra local-milvus netops-copilot doctor --profile local-milvus
~~~

首次运行时，如果还没有活动索引，报告中的 active-index 可能未就绪，这是正常的；Milvus、PostgreSQL 和 Embedding 配置应先处于可用状态。

### 5.3 构建并激活索引

~~~powershell
uv run --extra local-milvus netops-copilot index-build --profile local-milvus
~~~

该命令会读取 datasets/demo 下的可摄取内容，生成摄取清单，创建新的 Milvus 索引版本，写入 Dense 与 BM25 索引，校验数量和样本回读，并在校验通过后切换活动索引。

索引构建需要 Embedding 服务可访问。如果命令提示 Embedding 配置缺失，先检查 .env 中的 DASHSCOPE_API_KEY 以及网络连接。

### 5.4 再次检查就绪状态

~~~powershell
uv run --extra local-milvus netops-copilot doctor --profile local-milvus
~~~

当 Milvus、PostgreSQL、Embedding、Dense/BM25 检索和 active-index 均可用时，才进入 API 或 Streamlit 启动步骤。

### 5.5 启动 FastAPI

在一个终端窗口运行：

~~~powershell
uv run --extra local-milvus --with uvicorn python tools/run_local_milvus_api.py --host 127.0.0.1 --port 8000
~~~

验证服务：

~~~powershell
Invoke-RestMethod http://127.0.0.1:8000/health/live
Invoke-RestMethod http://127.0.0.1:8000/health/ready
~~~

浏览器打开 http://127.0.0.1:8000/docs 查看 Swagger UI，打开 http://127.0.0.1:8000/openapi.json 查看 OpenAPI 描述。

### 5.6 启动 Streamlit

在另一个终端窗口运行：

~~~powershell
uv run --extra local-milvus python -m streamlit run tools/run_local_milvus_streamlit.py --server.port 8502
~~~

浏览器打开 http://127.0.0.1:8502。页面包含知识检索、事故场景回放、配置差异比较、追踪与评测等工作区。

如果页面空白或一直停留在加载状态，按以下顺序检查：

~~~powershell
docker compose ps
uv run --extra local-milvus netops-copilot doctor --profile local-milvus
Invoke-RestMethod http://127.0.0.1:8000/health/ready
~~~

最常见原因是 Docker 未启动、Milvus/PostgreSQL 尚未就绪，或还没有完成 index-build。Streamlit 会在构造应用服务时连接这些依赖，依赖不可用时不会正常渲染业务页面。

## 6. 轻量运行：demo-lite

demo-lite 使用本地 Chroma、SQLite FTS5 和 SQLite 元数据，不需要启动 Docker。它适合快速检查 CLI、索引流程、API 和页面结构。

demo-lite 的索引构建同样需要 Embedding 服务配置；不需要 Docker，但仍需准备 DASHSCOPE_API_KEY 或接入其他兼容 provider。

~~~powershell
uv run --extra demo-index netops-copilot demo-lite
uv run --extra demo-index netops-copilot index-build --profile demo-lite
~~~

启动 API：

~~~powershell
uv run --extra demo-index --with uvicorn python tools/run_demo_api.py --host 127.0.0.1 --port 8000
~~~

启动 Streamlit：

~~~powershell
uv run --extra demo-index python -m streamlit run tools/run_demo_streamlit.py --server.port 8501
~~~

如果尚未构建索引，可以把 --build 传给项目启动脚本：

~~~powershell
uv run --extra demo-index --with uvicorn python tools/run_demo_api.py --build
uv run --extra demo-index python -m streamlit run tools/run_demo_streamlit.py -- --build
~~~

## 7. REST API 使用

### 健康检查

~~~powershell
Invoke-RestMethod http://127.0.0.1:8000/health/live
Invoke-RestMethod http://127.0.0.1:8000/health/ready
~~~

### 知识查询

~~~powershell
$body = @{
    query = "OSPF ExStart 状态需要检查哪些内容？"
    mode = "hybrid"
    limit = 5
} | ConvertTo-Json

Invoke-RestMethod -Method Post -Uri http://127.0.0.1:8000/api/v1/query -ContentType "application/json" -Body $body
~~~

可选检索模式：dense-only、lexical-only、hybrid 和 hybrid-rerank。请求还可以带 site_id、device_id、vendor、os_version 和 security_level 过滤条件。

### 配置差异比较

~~~powershell
$body = @{
    vendor = "Huawei"
    baseline = "interface GigabitEthernet0/0/1"
    current = "interface GigabitEthernet0/0/1 description uplink-prod"
} | ConvertTo-Json

Invoke-RestMethod -Method Post -Uri http://127.0.0.1:8000/api/v1/configs/diff -ContentType "application/json" -Body $body
~~~

### 可用接口

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| GET | /health/live | 进程存活检查 |
| GET | /health/ready | 依赖和活动索引就绪检查 |
| POST | /api/v1/query | 知识检索和候选证据返回 |
| POST | /api/v1/incidents/diagnose | 事故证据研判 |
| POST | /api/v1/configs/diff | 配置差异比较 |
| GET | /api/v1/devices/{device_id}/context | 读取设备上下文 |
| GET | /api/v1/sources | 读取知识来源元数据 |
| POST | /api/v1/ingestion | 提交摄取请求 |
| GET | /api/v1/evaluation | 读取评测摘要 |
| GET | /api/v1/traces/{trace_id} | 读取调用追踪 |

实际可用的业务处理器由运行时组合决定；以 /health/ready、/docs 和接口返回的 service_unavailable 状态为准。

## 8. CLI 命令说明

| 命令 | 作用 |
| --- | --- |
| doctor --profile <profile> | 检查 provider、数据库、索引和可选能力 |
| index-build --profile <profile> | 构建并激活指定 profile 的索引版本 |
| demo-lite | 重置运行时、重新生成数据并执行摄取 |
| seed | 重新生成项目数据目录 |
| ingest | 读取数据目录并写入摄取清单 |
| reset | 删除 datasets/demo/.runtime 下的运行时状态 |
| local-milvus | 执行 local-milvus 就绪检查 |

默认数据目录是 datasets/demo，默认运行时目录是 datasets/demo/.runtime。需要指定其他目录时使用 --dataset-root 和 --runtime-dir。

## 9. MCP stdio

项目提供受限的 MCP stdio 适配器：

~~~powershell
uv run python tools/run_mcp_stdio.py
~~~

该入口只注册命名工具，不提供 shell、原始 SSH、设备写入或任意方法分派能力。要接入具体应用，需要在宿主侧将应用处理器显式注入 create_mcp_server；直接运行默认入口只用于协议层检查。

## 10. 目录结构与扩展位置

~~~text
config/profiles/                    运行 profile 配置
datasets/demo/                      数据集、知识文档和索引输入
src/netops_copilot/domain/          领域模型和业务概念
src/netops_copilot/application/     用例、索引、检索、授权和安全策略
src/netops_copilot/infrastructure/  Milvus、Chroma、PostgreSQL、SQLite 和模型适配器
src/netops_copilot/interfaces/      CLI、FastAPI、Streamlit、MCP 入口
tests/                              单元、契约和集成测试
tools/                              数据生成、启动和验收脚本
docker-compose.yml                  local-milvus 的基础设施编排
~~~

新增功能时建议遵循以下边界：

1. 领域规则放在 domain，不要直接依赖 FastAPI、Streamlit 或数据库客户端。
2. 用例编排放在 application，通过 ports 接口依赖外部能力。
3. 具体数据库、向量库、模型和设备连接放在 infrastructure。
4. API、CLI、Streamlit 和 MCP 只负责参数转换、依赖注入和结果呈现。
5. 任何新数据源都应经过解析、规范化、敏感字段处理、分块和索引版本校验。

## 11. 测试与代码检查

在项目根目录运行：

~~~powershell
uv run python -m unittest discover -s tests
uv run ruff check src tests tools
uv run python tools/check_architecture.py
uv run python tools/check_repository_assets.py
uv run python tools/generate_demo_dataset.py --check
~~~

如果本机已启动 Docker，并且 .env 和 Embedding 服务均可用，可以执行 local-milvus 相关验收：

~~~powershell
uv run --extra local-milvus python tools/run_acceptance.py --profile local-milvus
~~~

本地完整验收会访问 Milvus、PostgreSQL 和 Embedding 服务；服务未启动时应先完成第 5 节。

## 12. 关闭与数据保留

停止应用进程使用 Ctrl+C。停止 Docker 服务：

~~~powershell
docker compose stop
~~~

下次继续使用时执行 docker compose start 或 docker compose up -d。

如果只想移除容器但保留数据库和索引卷：

~~~powershell
docker compose down
~~~

不要在不了解后果时使用 docker compose down -v；-v 会同时删除 Compose 管理的 Milvus、MinIO、etcd 和 PostgreSQL 数据卷。
